## Jalankan Secara Lokal

**Prasyarat:**  Node.js

1. Instal dependensi:
   `npm install`
2. Atur `GEMINI_API_KEY` di [.env.local](.env.local) dengan kunci API Gemini Anda
3. Jalankan aplikasi:
   `npm run dev`
